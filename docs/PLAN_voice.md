# 読み上げの声 — 提案

2026-08-30 / 現状: ブラウザの `SpeechSynthesis`（機械音声）

## いまの何が問題か

- **端末で声も読み方も変わる。**iOS/Android/PCで別人が読む。無い環境では黙る
- **遅い。**端末によっては押してから鳴るまで待つ。幼児は待てない
- **「あ、ありの あ」を正しく読まない**ことがある（区切り・アクセント）
- オフライン対応を入れたのに、**声だけはネットにも端末にも依存したまま**

## 何を焼くか

読み上げ文は `app/js/words.js` の `readingOf()` が全字ぶん持っている。**台本を書く作業は要らない。**

| 中身 | 本数 | 例 |
|---|---|---|
| ひらがな 清音 | 46 | 「あ、ありの あ」 |
| カタカナ 清音 | 46 | 「ア、アイスの ア」 |
| ひらがな だくおん・ちいさいじ | 35 | |
| カタカナ だくおん・ちいさいじ | 36 | |
| きまり文句 | 約15 | 「かんぺき！」「さいごまで なぞろう」「ステゴサウルスの しっぽを みつけた！」…※ |
| **計** | **約178＋15** | |

※ ホネの獲得は「恐竜名＋部位名」の組み合わせなので、
**恐竜名（6）と部位名（5＋特注3）を別々に焼いて繋ぐ**のが安い。全組み合わせを焼かない。

## 容量（ここが決めどころ）

いまアプリは **1.56MB**。検査で **2MBを上限**にしている。

| やり方 | 目安 |
|---|---|
| mp3 32kbps mono | 約1.5MB → **合計3.1MB（上限超え）** |
| **Opus 24kbps mono** | 約1.2MB → 合計2.7MB |
| **Opus 16kbps mono**（音声には十分） | 約0.8MB → 合計2.4MB |
| 清音92本＋きまり文句だけ焼く | 約0.45MB → **合計2.0MB** |

**推奨: Opus 16kbps ＋ 1本にまとめた「音声スプライト」。**
193本を1ファイルに繋いで、再生位置で切り出す。
リクエストが1本で済み、無音のつなぎ目がよく縮むので、バラで持つより小さくなる。
再生はすでにある `app/js/audio.js` の Web Audio に乗せる
（`<audio>` を193個持つと iOS で重い。1つのバッファをデコードして region 再生が確実）。

上限は **2.5MB に上げる**か、**第1弾は清音92本だけ焼いて残りは今の機械音声に落とす**か。
後者なら上限を触らずに済み、いちばん効くところ（毎日出る字）から良くなる。

## 誰の声か（2026-08-31 方針が決まった）

**よこぼの声は使わない**（一般ユーザー向けにはウケが良くない、との判断）。
**VOICEVOX** で、いまの機械音声より親しみやすいトーンにする。

**この端末に VOICEVOX がすでに入っている**
（`AppData/Local/Programs/VOICEVOX/vv-engine/run.exe`）。
ヘッドレスで起動すれば `http://127.0.0.1:50021` から焼ける。
**ローカル・無料・無制限・オフライン・APIキー不要。**43人・127スタイル。

同じセリフで6人ぶん焼いて聞き比べられるようにした → `work/voice-test/voices.html`
（`node work/voice-test/make.mjs` で作り直せる）。候補:

| 話者 | スタイル | 印象 |
|---|---|---|
| 四国めたん | あまあま(0) | やわらかい・甘め |
| 春日部つむぎ | ノーマル(8) | 明るい・元気 |
| 冥鳴ひまり | ノーマル(14) | おだやか・落ち着き |
| 九州そら | あまあま(15) | ゆっくり・やさしい |
| ずんだもん | あまあま(1) | 知名度が高い（好みが割れる） |
| もち子さん | ノーマル(20) | 素直・中庸 |

読み上げは **0.95倍** に落としてある（幼児向け）。

**クレジット表記が要る**（例「VOICEVOX:春日部つむぎ」）。キャラごとに条件が違うので、
**決めたら利用規約を確認してから焼く。**画面のどこに出すかも決める（`credit` の行が使える）。

## 容量（実測しなおした）

春日部つむぎ・opus 24kbps・0.95倍で実測:

| 1本あたり | mp3 32k | opus 24k | opus 16k |
|---|---|---|---|
| 字だけ「あ」 | 2.1KB | **1.1KB** | 0.8KB |
| 字＋ことば「あ。ありの、あ」 | 9.4KB | 5.9KB | 3.9KB |
| きまり文句「かんぺき！」 | 3.8KB | 2.4KB | 1.7KB |

| 焼く範囲 | 音声 | アプリ合計 |
|---|---|---|
| 清音92字（字だけ）＋文句15 | 138KB | 1.84MB |
| **全178字（字だけ）＋文句15** | **234KB** | **1.93MB**（上限2MB内） |
| 全178字（字＋ことば）＋文句15 | 1085KB | 2.76MB（上限超え） |

**推し: 全178字の「字だけ」＋きまり文句 = 234KB。**
ことば部分（「ありの あ」）は 1085KB と高い割に、効果の大半は「字の音」で取れる。
まず字だけ置きかえて、🔊 ボタンのことばは今の機械音声に残す。
足りなければ、あとから清音92字ぶんだけ足す（+543KB、上限を2.5MBに上げる必要あり）。

## 作りかた（決まったあと）

1. `tools/build_voice.mjs` — `words.js` から読み上げ文の一覧を出す（録音台本 / TTS入力）
2. 録音 or TTS → `work/voice/` に1本ずつ
3. `tools/pack_voice.mjs` — 無音を詰めて1本のOpusに繋ぎ、`app/assets/voice.opus` と
   `app/assets/voice.json`（どの字がどの位置か）を書き出す
4. `app/js/audio.js` に `speak(ch)` を足す。**ファイルが無い/読めないときは今の
   `SpeechSynthesis` に落ちる**（すでにその構造がある）
5. `sw.js` が自動で拾う（`build_sw.mjs` が app/ を歩くので、置くだけでオフラインにも入る）
6. 検査: 全字ぶん位置があるか / 音が0.3秒以上あるか（無音の焼き損じ）/ 合計サイズ

## 決めてほしいこと

1. **どの話者・スタイルか**（`work/voice-test/voices.html` を聞いて1人選ぶ）
2. **焼く範囲**（推し: 全178字の字だけ＋きまり文句＝234KB。上限を触らずに済む）
3. クレジット表記を**どこに出すか**（いまの `KanjiVG` の行に並べるのが素直）
